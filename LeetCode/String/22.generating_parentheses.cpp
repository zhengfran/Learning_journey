#include <string>
#include <vector>

using namespace std;

class Solution {
public:
    vector<string> generateParenthesis(int n) {
        vector<string> res;
        int bal=0;
        for (int i=0;i<2*n;i++){
            if (bal==0){
                //this looks like an brutal force solution, need to think of a better one
            }
        }
        
    }
};